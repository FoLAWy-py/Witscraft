# TYPE      DATABASE  USER  ADDRESS             METHOD
local       all       all                       trust
hostssl     all       all   127.0.0.1/32        scram-sha-256
hostssl     all       all   ::1/128             scram-sha-256
hostssl     all       all   192.168.31.0/24     scram-sha-256
hostssl     all       all   10.0.0.0/24         scram-sha-256
# Docker rewrites host-loopback traffic to a private bridge gateway. The backend
# port is loopback-only, Nginx filters original client sources, and PostgreSQL
# still requires TLS and SCRAM on every bridge address.
hostssl     all       all   172.16.0.0/12       scram-sha-256
hostssl     all       all   192.168.65.0/24     scram-sha-256
hostnossl   all       all   0.0.0.0/0           reject
hostnossl   all       all   ::0/0               reject
host        all       all   0.0.0.0/0           reject
host        all       all   ::0/0               reject
